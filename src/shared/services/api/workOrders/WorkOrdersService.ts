import { Environment } from '../../../environment';
import { Api } from '../axios-config';

const create  = async () => {
  console.log('INIT WORKORDERS SERVICE', 'INIT');

  try {
    //
  } catch (e) {
    //
  }
};


export const WorkOrdersService = {
  create
};